# Лабораторная работа 4

Моисеенко Милена Алексеевна, группа М8О-101БВ-25

* **Цель:**  Научиться реализовывать приложения с асинхронной моделью управления. Реализовать асинхронный исполнитель задач, обрабатывающий задачи из очереди с использованием расширяемых обработчиков.
* **Библиотеки:** *asyncio*, *logging*, *typing*, *typer*, *json*, *pathlib*, *datetime*, *pytest*
* **Допущения:**
  - отсутствуют

* **Чему я научилась:**
    - Создавать асинхронные очереди в python.
    - Создавать свои обработчики.
    - Использовать инструменты асинхронного программирования для ускорения обработки данных.

## Описание решения
В этой ЛР оснвной упор делается на реализацию хэндлеров (обработчиков), выполняющих единый контракт, и класса асинхронного исполнителя задач. Опишу основные компоненты своего решения:

### Обработчики (хэндлеры)
Я описала единый контракт для обработчиков, он подразумевает, что хэндлер должен реализовывать 3 метода: handle (обработка задач), setup (настройка на старте) и cleanup (очистка по завершении). На основе этого протокола я создала 4 обработчика:

- LoggingHandler - Обработчик, который просто логирует каждый шаг. Остальные обработчики тоже логируют шаги, но этот не делает ничего более.

- PriorityBasedHandler - Обработчик, который делает время обработки задачи пропорциональной ее приоритету.

- SimulateWorkHandler - Обработчик, демоснтрирующий реальную обработку задач. Он меняет статусы задач, а также делает время их обработки не фикисированным, а случайным в определенном диапазоне.

- RobustHandler - Обработчик с встроенной обработкой ошибок и повторными попытками.

Архитектура позволяет спокойно добавить новые типы обработчиков, если те выполняют контракт обработчика.

### Класс AsyncTaskExecutor

Этот класс представляет собой асинхронный исполнитель задач. Ему можно задать обработчик, который будет использоваться при обработке задач, максимальный размер очереди задач, имя и количество воркеров. Он также использует контекстные менеджеры для управления ресурсами. Также есть защита от бесконечного цикла в event loop, нет блокирующих операций.

### Централизиванное логирование
Каждый тип хендлера делает логирование операций и ошибок, файлы с логами одновременно выводятся в консоль и в соответствующий файл в папке logs.

### cli-интерфейс

Для создания cli-интерфейса в этой ЛР была использована бибилотека typer. Этот интерфейс позволяет продемонстрировать все основные возможности программы. Большинство его команд уже были созданы в предыдущих ЛР, а в ходе выполнения этой работы в него было добавлено 2 новых команды для демонстрации работы асинхронного обработчика.

## Тесты

Для этой ЛР были написаны авто-тесты с помощью pytest, проверяющие корректность работы AsyncTaskExecutor и классов хэндлеров. Для ручных тестов работы программы в репозитории также есть 2 прмиера jsonl файлов с задачами, чтобы пользователь быстро мог протестировать программу самостоятельно.

## Инструкция к использованию

### Установить репозиторий

```git clone https://github.com/miniMlena/sem2_python_lab4```

```cd sem2_python_lab4```

### Запустить
После установки репозитория есть 2 способа запуска программы:

### Способ №1: с использованием Dockerfile

Соберите образ: ```docker build -t lab4_python .```

Запустите: ```docker run -it --rm lab4_python```

Вы увидите приветственное сообщение с инструкцией, но информация оттуда продублирована и тут. Теперь вы можете запускать тесты или саму программу.

- Для запуска тетсов введите: ```pytest```
- Для запуска программы введите: ```python -m src```
- Чтобы посмотреть подробную инструкцию к командам самой программы с примерами запустите программу и введите: ```instruct```
- Команды для самой программы вводите согласно этой инструкции. В разделе "Синтаксис" ниже также есть та же самая инструкция.
- Для выхода из программы введите: ```exit```
- Для выхода из контейнера также введите: ```exit```

### Способ №2: без dockerfile
После установки репозитория введите в терминале интересующую вас команду:

- Запустить тесты: ```pytest```
- Запустить программу: ```python -m src```
- Инструкция к программе: ```instruct```
- Вводите команды согласно этой инструкции, например можете начать с интерактивной демонстрации работы асинхронного обработчика: ```async-interactive```
- Для выхода введите ```exit```

Для просмотра подробной инструкции к вводу команд в самой программе, введите ```instruct``` или посмотрите раздел "Синтаксис" ниже.

### Синтаксис
В самой программе доступны следующие 17 команд:

1. instruct - Выводит это сообщение
    У этой команды нет опций или аргументов
    
    Пример: ```instruct```

2. sources - Выводит список доступных источников задач
   
   Пример: ```sources```

3. read - Читает и показывает задачи из указанных источников с возможностью фильтрации
    
    Аргументы и опции:
     
     --stdin              - Считывать задачи из стандартного ввода
     
     --max-tasks N        - Максимальное число задач (0 = без ограничения)
     
     --author TEXT        - Фильтр по автору
     
     --contains TEXT      - Фильтр по содержанию
     
     file1.jsonl          - Пути к JSONL файлам
    
    Подробно про аргументы и опции команды read:
          
    В качестве аргументов можно указать файлы формата jsonl, тогда задачи будут читаться из них.
    Примеры:
   ```
    read example_1.jsonl
   ```
   ```
    read example_1.jsonl example_2.jsonl
   ```
          
    --stdin - Считывать задачи из сдандартного ввода
    Задачи нужно писать в формате id:title:author:content:priority(число)
    Если вы захотите перестать вводить задачи, напишите stop
    
    Пример использования:
   ```
    read --stdin
   ```
    Дальше вводим задачи, например:   my_id:my_title:my_author:my_content:1
          
    --max_tasks - Установить максимум, сколько задач может быть прочитано
    По умолчанию 10, если поставить 0, то можно прочитать неограниченное число задач.
    При одновременном вводе --max_tasks 0 и --stdin не забывайте про возможность завершить ввод задач командой stop.
    
    Примеры:
   ```
    read --stdin --max-tasks 5
   ```
   ```
    read example_1.jsonl --max-tasks 0
   ```
          
    --author - Считывать задачи только указанного автора
    
    Пример:
   ```
    read --stdin --author milena
   ```
    Все встреченные задачи с другим автором не будут считываться программой
          
    --contains - Считывать только задачи с указанной подстрокой в содержании
    Пример:
   ```
    read example_1.jsonl --contains проект
   ```
    Задача '8:Сделать бэкап:milena:Сделать бэкап базы данных проекта:2' будет считана"
    
    Задача '9:Написать тесты:cool_user:Написать тесты к лабе по python:3' не считается"
          
    Все аргументы и опции можно комбинировать, примеры:
   ```
    read example_1.jsonl example_2.jsonl --max-tasks 20 --author kate
   ```
   ```
    read example_1.jsonl example_2.jsonl --stdin --max-tasks 20
   ```
   ```
    read example_1.jsonl --stdin --author alina --contains python
   ```

4. start TASK_ID - Начинает выполнение задачи с указанным ID
   
   Пример: ```start T-001```

5. review TASK_ID - Отправляет задачу с указанным ID на ревью
   
   Пример: ```review PY_002```

6. complete TASK_ID - Завершает задачу с указанным ID
   
   Пример: ```complete C_006```

7. archive TASK_ID - Архивирует задачу с указанным ID
   
   Пример: ```archive T-007```

8. info TASK_ID - Показывает подробную информацию о задаче с указанным ID
   
   Пример: ```info A111```

9. list - Показывает список всех задач
   
   Пример: ```list```

10. create - Создает новую задачу в интерактивном режиме
    
    Пример: ```create```

11. queue-init - Создание очереди с указанными источниками
    Аргументами являются jsonl-файлы. Очередь единая на всю программу, при повторном вводе этой команды очередь перезаписывается.
    
    Пример:
    ```
    queue-init example_1.jsonl
    ```
          
12. queue-demo - Быстро демонстрирует основные операции с очередью
    
    Пример: ```queue-demo```
          
13. queue-filter - Фильтрует задачи в очереди по указанным критериям
    
    Опции:
    
    --status - фильтр по статусу
    
    --min_priority - фильтр по минимальному приоритету
    
    --author - фильтр по автору
    
    --contains - фильтр по подстроке в названии задчи
    
    Опции можно комбинировать.
    
    Примеры:
    ```
    queue-filter --contains дз
    ```
    ```
    queue-filter --author kate --status done
    ```
    ```
    queue-filter --min-priority 2 --author milena --contains от
    ```
          
14. queue-process - Демонстрирует потоковую обработку задач
    
    Пример: ```queue-process```

15. async-demo - Демонстрация работы асинхронного исполнителя
    Пример: ```async-demo```

16. async-interactive - Переход в режим интерактивной асинхронной обработки.
    В этом режиме доступно несколько команд:

    add \<id\> \<title\> [priority] - Добавить задачу
    
    add-test \<n\> - Добавить n тестовых задач

    status - Показать статус обработки

    workers \<n\> - Изменить количество воркеров (будет перезапуск)

    handler \<type\> - Сменить обработчика (logging/priority/work/robust)

    stop - Остановить обработку и выйти

    Пример:
    ```
    async-interactive
    add-test 8
    status
    stop
    ```

17. exit - Выход из программы
    
    Пример: ```exit```